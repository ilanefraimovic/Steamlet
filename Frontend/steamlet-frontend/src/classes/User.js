class User {
    // Constructor
    constructor(userID, username) {
        this.userID = userID;  // Attribute to store user ID
        this.username = username;  // Attribute to store username
    }

    // Method to get all Sets -> Returns List of Sets
    getAllSets() {
        // IMPLEMENT
        return [];
    }
}
